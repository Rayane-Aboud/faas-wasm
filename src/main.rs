mod node;
mod supervisor;
mod utils;
mod task;
mod config;
mod repositories;
use anyhow::Result;



fn main() -> Result<()> {
  
  Ok(())
}

/*
scheduler that runs wasm files while knowing their resources
*/